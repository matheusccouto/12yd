## System environment and global preferences:
- GitHub CLI (gh) is installed and authenticated as matheusccouto
- Preferred programming language: Python (use uv for package management, ruff for linting/formatting, and ty for static typing)


## Agent skills

### Issue tracker

GitHub Issues for `matheusccouto/12yd` (uses the `gh` CLI). External PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Defaults: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` at the root, `docs/adr/` for ADRs. See `docs/agents/domain.md`.
