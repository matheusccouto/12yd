# Streamlit Cloud deploy manifest. Hand-maintained, mirrors
# pyproject.toml::[project.dependencies]. Do NOT regenerate with
# `pip freeze > requirements.txt` — Streamlit Cloud reads this
# file's hash to decide whether to do a fresh install; a
# regenerate after every dep bump would mask the real cause of a
# broken deploy and pin versions that diverge from the local
# `uv`-managed dev env. See Issue #52.
-e .
httpx>=0.27
huggingface_hub>=0.20
numpy>=2.0
pandas>=2.0
packaging>=24.0
plotly>=6.0
scikit-learn>=1.5
lightgbm>=4.5
streamlit>=1.30
