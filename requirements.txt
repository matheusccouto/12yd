# Streamlit Cloud deploy manifest — the hand-maintained mirror of pyproject.toml.
# Keep in sync with [project.dependencies] + [dependency-groups].app + [dependency-groups].pipeline.
# Do NOT regenerate with `pip freeze` — the == pins would break the deploy hash-detection
# and mask future drift between requirements.txt and pyproject.toml.

httpx>=0.27
numpy>=2.0
pandas>=2.0
streamlit>=1.30
tabpfn-client>=0.3
